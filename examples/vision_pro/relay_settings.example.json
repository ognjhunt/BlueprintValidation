{
  "relay_host": "GPU_BOX_IP_OR_HOSTNAME",
  "relay_port": 49111,
  "teleop_device": "vision_pro",
  "packet_mode": "right_hand_pose_with_pinch",
  "notes": [
    "relay_host and relay_port must match the remote GPU box values used by run_vision_pro_relay.sh",
    "packet_mode is informational; the current recorder relay accepts direct 7D actions, ee_delta_pose+gripper, or right_hand pose packets"
  ]
}
